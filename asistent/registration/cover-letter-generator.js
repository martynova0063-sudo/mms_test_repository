const baseTemplate = `Добрый день!

Мой коммерческий опыт работы в разработке на Python — 3 года, на Java — 6 лет. Работаю с нейросетями, около 10 лет — с различными базами данных. Прошла обучение и получила сертификаты: AI/ML‑разработчика, создание AI‑агента и дообучение RAG. Владею профильным программированием на JavaScript, CSS, HTML. Знаю фреймворки: Spring Boot, JMIX, FastAPI, Django, React, Node.js.

Есть опыт работы с Linux и Unix‑системами, Kafka, Jenkins, Grafana, JasperReport Studio, Docker, Portainer, микросервисной архитектурой и развёртыванием контейнеров, DevOps‑инжинирингом. Системы контроля версий: Git, TFS.

На предыдущих местах работы участвовала в проектах по созданию нейросетей, AI‑агентов, систем компьютерного зрения, занималась разработкой серверного обеспечения на Python/Java, переходом на новые платформы, стабилизацией версий, написанием отчётов, сервисов и импортом данных. Есть опыт работы в банках, внедрения АБС.

На текущем месте много работала с Java‑стеком и закрыла несколько крупных задач по оптимизации производительности. Сейчас хочу развиваться дальше: брать более сложные архитектурные задачи и активнее использовать Python для автоматизации и анализа данных. Ищу проект, где смогу применять оба языка и расти как full stack разработчик в бэкенде.

Могу предоставить благодарственные письма и рекомендации.
Рассматриваю трудоустройство по ТК РФ, ИП или договору ГПХ.

Благодарю за внимание к моему резюме. Готова пройти собеседование по телефону, онлайн или другим удобным для вас способом.

С уважением, Мария.`;

// Ключевые слова для классификации вакансий
const roleKeywords = {
  java: ['java', 'spring', 'jvm', 'backend', 'микросервисы', 'производительность'],
  python: ['python', 'fastapi', 'django', 'анализ данных', 'автоматизация', 'ml', 'ai', 'data'],
  fullstack: ['full stack', 'full-stack', 'frontend', 'react', 'node.js', 'web'],
  devops: ['devops', 'ci/cd', 'kubernetes', 'docker', 'инфраструктура', 'мониторинг'],
  ml_ai: ['ml', 'machine learning', 'ai', 'нейросеть', 'компьютерное зрение', 'rag', 'llm'],
};

/**
 * Определяет роль по заголовку вакансии (возвращает массив тегов)
 */
function classifyVacancy(title) {
  const lower = title.toLowerCase();
  const tags = new Set();

  for (const [role, keywords] of Object.entries(roleKeywords)) {
    if (keywords.some(k => lower.includes(k))) {
      tags.add(role);
    }
  }

  // Если ничего не подошло — считаем общим бэкендом
  if (tags.size === 0) {
    tags.add('java'); // по умолчанию упор на Java-опыт
  }

  return Array.from(tags);
}

/**
 * Формирует персонализированное сопроводительное письмо
 * @param {string} vacancyTitle - заголовок вакансии
 * @param {Object} contactInfo - { phone, email, telegram, maxLink }
 * @returns {string} - готовое письмо
 */
function generateCoverLetter(vacancyTitle, contactInfo) {
  const tags = classifyVacancy(vacancyTitle);
  let text = baseTemplate;

  // Здесь можно добавлять вставки в зависимости от роли
  if (tags.includes('ml_ai')) {
    text += `\n\nОсобенно интересен опыт в AI/ML: участвовала в создании нейросетей и AI‑агентов, дообучении RAG, системах компьютерного зрения. Готова применять эти навыки в продуктовых задачах.`;
  } else if (tags.includes('devops')) {
    text += `\n\nЕсть практический опыт в DevOps‑инжиниринге: CI/CD, контейнеризация, мониторинг (Grafana), работа с инфраструктурой. Могу участвовать в улучшении стабильности и наблюдаемости сервисов.`;
  } else if (tags.includes('python')) {
    text += `\n\nАктивно использую Python для автоматизации, анализа данных и бэкенд‑сервисов (FastAPI/Django). Готова брать задачи, где Python — основной стек.`;
  } else if (tags.includes('fullstack')) {
    text += `\n\nИмею опыт полного цикла разработки: от фронтенда (React, HTML/CSS/JS) до бэкенд‑логики и интеграции с БД. Готова участвовать во всех слоях приложения.`;
  }

  text += `\n\nКонтакты для связи:\nТелефон: ${contactInfo.phone}\nEmail: ${contactInfo.email}\nTelegram: ${contactInfo.telegram}\nMAX: ${contactInfo.maxLink}`;

  return text;
}

export { generateCoverLetter, classifyVacancy };


/*
Что можно улучшить (автоматизация, Playwright, экспорт в Excel, работа с БД и т. д.):

Шаблоны из БД или JSON. Вместо жёстко прописанных текстов хранить варианты шаблонов в JSON или базе и подгружать нужный по тегам. Это удобно, если шаблонов много.
Интеграция с Playwright. Если ты собираешь заголовки вакансий автоматически (например, со страницы hh.ru), можно сразу передавать vacancyTitle в generateCoverLetter и сохранять результат в CSV/Excel (как в твоих прошлых задачах по экспорту).
Тонкая персонализация. Можно добавить поле «ключевая задача из описания вакансии» и вставлять 1–2 предложения про релевантный опыт именно под эту задачу.
Локализация/разные версии. Можно сделать отдельные шаблоны для банков/финтеха, энтерпрайза, стартапов, чтобы подчёркивать разные кейсы (АБС, производительность, быстрые релизы и т. п.).
 */